<OL id="talk">
                    <%
					 i = 0
					 commit.each do |m| 
					 i += 1
					 %>
                      <LI id="ev<%= m.id %>">
                        
                        <DIV class="article">
                        <P class="picture"> <SPAN class="thumbpic"><fo:profile-pic uid="<%= m.sns_user_xid %>" linked="true" /></SPAN> </P>
                        
                        <div class="info">
                          <SPAN class="author"><fo:name uid="<%= m.sns_user_xid %>"  linked="true"/>
                          </SPAN>
                          
                            <div class="switch"></div>
                          <SPAN class="time">
                          <%= m.created_at.strftime("%m-%d %H:%M") %>
                          </SPAN> <a href="#message" onclick="reply_message('<%= m.sns_user_xid %>','<%= m.id %>')">回复</a></DIV>
                        
                         
                        <DIV class="text"><%=h m.content %></div>
                        </DIV>
                      </li>
                    <%
					if controller.action_name == "show" and i == 10
						break
					end
					 end %>
                  </OL>  